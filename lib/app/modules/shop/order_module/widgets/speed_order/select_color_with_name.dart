import 'package:flutter/material.dart';

class SelectColorWithName extends StatelessWidget {
  const SelectColorWithName({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
